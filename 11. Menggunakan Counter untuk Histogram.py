import collections

# Membuat histogram dari sejumlah nilai
values = [1, 2, 2, 3, 3, 3, 4, 4, 4, 4]
histogram = collections.Counter(values)

print("Histogram dari nilai:", histogram)
# Fungsi: Membuat histogram dari nilai.
# Kondisi: Ketika Anda ingin menghitung frekuensi munculnya setiap nilai.
